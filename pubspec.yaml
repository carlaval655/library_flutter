name: my_movie_tracker
description: App para tracking de películas y series.

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^2.5.1
  sqflite: ^2.3.2
  path_provider: ^2.1.2
  supabase_flutter: ^2.5.3
  dio: ^5.4.0
  image_picker: ^1.1.0
  go_router: ^13.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter